Fleet running on Meteor

### Additional Node Packages Required
* colors
* node-sqlserver

Uses Meteor, Leaflet with Clustering and a little bit of (magic) propriatory communication code. (Cannot share this)
